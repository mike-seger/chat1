spring:
    datasource:
        url: jdbc:h2:~/h2/chat1;MODE=MySQL;DB_CLOSE_ON_EXIT=FALSE;AUTO_SERVER=TRUE
    output:
        ansi:
            enabled : always
    jpa:
        hibernate:
            ddl-auto: update